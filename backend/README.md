# backend

Phase 2. FastAPI + Postgres sync API for Ledgerly (see `docs/design-spec.md`, Section 4).
Endpoints: `/auth/login`, `/auth/google`, `/auth/refresh`, `/sync/time`, `/sync/push`, `/sync/pull`, `/healthz`.
Nothing here is built in phase 1; the folder exists so the layout is settled from day one.
