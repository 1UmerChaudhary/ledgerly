targets:
  $default:
    builders:
      drift_dev:
        options:
          sql:
            dialect: sqlite
            options:
              version: "3.45"
              modules: [json1, fts5]
          store_date_time_values_as_text: true
          generate_connect_constructor: false
